# Environment variables for the OpenAI API Proxy

# Required: Your OpenAI API key
OPENAI_API_KEY=your-api-key-here

# Optional: OpenAI base URL (default: https://api.openai.com)
# OPENAI_BASE_URL=https://api.openai.com

# Optional: Proxy server port (default: 8000)
# PROXY_PORT=8000

# Optional: Request timeout in seconds (default: 300)
# PROXY_TIMEOUT=300

# Storage Configuration
# STORAGE_TYPE: "local" or "azure" (default: local)
STORAGE_TYPE=local

# Local storage configuration (used when STORAGE_TYPE=local)
# Directory where log files will be stored (default: logs)
LOCAL_LOG_DIR=logs

# Azure Blob Storage configuration (used when STORAGE_TYPE=azure)
# Required when STORAGE_TYPE=azure
# AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name
# AZURE_STORAGE_ACCOUNT_KEY=your-storage-account-key
# AZURE_STORAGE_CONTAINER=proxy-logs

# Log files are named: {model}_{YYYYMMDD}.jsonl
# Example: gpt-4_20241021.jsonl
